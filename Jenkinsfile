// Powered by Infostretch 

timestamps {

node () {

	stage ('ci_front_taras - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/main']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'myronovich-github-user-token', url: 'https://github.com/myronovich/demo3']]]) 
	}
	stage ('ci_front_taras - Build') {
 	
// Unable to convert a build step referring to "org.jenkinsci.plugins.dockerbuildstep.DockerBuilder". Please verify and convert manually if required.
// Unable to convert a build step referring to "org.jenkinsci.plugins.dockerbuildstep.DockerBuilder". Please verify and convert manually if required. 
	}
}
}